<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.peng.hwmallservicedemo.repository.user.UserRepository">

    <!-- 统一字段映射，确保 user_id 正确填充 -->
    <resultMap id="UserResultMap" type="com.peng.hwmallservicedemo.model.user.User">
        <id column="user_id" property="user_id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
    </resultMap>

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT user_id, username, password, phone, email
        FROM user
        WHERE username = #{username}
    </select>

    <!-- 根据用户ID查询用户 -->
    <select id="findById" parameterType="int" resultMap="UserResultMap">
        SELECT user_id, username, password, phone, email
        FROM user
        WHERE user_id = #{user_id}
    </select>

    <!-- 检查用户名是否存在 -->
    <select id="countByUsername" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM user 
        WHERE username = #{username}
    </select>

    <!-- 保存用户信息（注册） -->
    <insert id="save" parameterType="com.peng.hwmallservicedemo.model.user.User" useGeneratedKeys="true" keyProperty="user_id">
        INSERT INTO user (username, password, phone, email) 
        VALUES (#{username}, #{password}, #{phone}, #{email})
    </insert>

    <!-- 更新用户信息 -->
    <update id="updateUser" parameterType="com.peng.hwmallservicedemo.model.user.User">
        UPDATE user 
        SET username = #{username},
            password = #{password},
            phone = #{phone},
            email = #{email}
        WHERE user_id = #{user_id}
    </update>

</mapper>
